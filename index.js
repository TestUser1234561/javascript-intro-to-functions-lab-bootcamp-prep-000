function shout (str) {
  return str.toUpperCase();
}

whisper = (str) => {
  return str.toLowerCase();
}

logShout = (str) => {
  console.log(str.toUpperCase());
}

logWhisper = (str) => {
  console.log(str.toLowerCase());
}
