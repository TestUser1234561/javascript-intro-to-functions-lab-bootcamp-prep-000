function shout(string) {
  return string
}

whisper = (str) => {
  return str.toLowerCase();
}

logShout = (str) => {
  console.log(str.toUpperCase());
}

logWhisper = (str) => {
  console.log(str.toLowerCase());
}
